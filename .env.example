# Claude API key for the optional LLM repair/fallback tiers.
# Without it, run with --llm off (the default): extraction and validation
# work fully offline; unresolved cells stay flagged in the workbook.
ANTHROPIC_API_KEY=sk-ant-...
